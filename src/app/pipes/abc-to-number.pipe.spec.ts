import { AbcToNumberPipe } from './abc-to-number.pipe';

describe('AbcToNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new AbcToNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
